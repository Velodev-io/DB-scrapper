# Handoff: Centered Back button on creation forms & auto-close details modal on save

All objectives are successfully resolved and verified via the production build output:

1. **Submit Property/Labour/Shop Pages**: Added centered back navigation buttons directly below the main page title headings.
2. **Auto-close Modals**: Updated `handleSaved` in `PropertyList.tsx`, `LabourList.tsx`, and `ShopList.tsx` to set the selected item state to `null`, ensuring the bottom modal sheet closes automatically when changes are saved.
3. **No git commit/push/deployment** operations were executed.
